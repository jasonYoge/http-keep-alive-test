const assert = require('assert');
const httpKeepAliveTest = require('../index.js');

describe('httpKeepAliveTest', () => {
  it('has a test', () => {
    assert(false, 'httpKeepAliveTest should have a test');
  });
});
